# Frontend environment variables (example)
#
# Do NOT commit real secrets.
# For local dev, create `frontend/.env.local` (gitignored) or set vars in your shell.
#

# NextAuth (only if you still use NextAuth routes)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="CHANGE_ME_DEV_ONLY"

# When running in docker-compose, the frontend should talk to same-origin /api in production.
# In local dev, Next.js rewrites proxy /api -> backend (see next.config.js).
# IMPORTANT: must stay same-origin in production (cookies/CSRF).
# Do NOT set an absolute backend URL here.
NEXT_PUBLIC_API_URL="/api"

# Optional: demo switches (avoid using in production)
DISABLE_AUTH="false"

